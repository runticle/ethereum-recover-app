import styled from "styled-components/native";

const Container = styled.View`
    flex: 1;
    background-color: green;
`

// todo get colours
const Colours = {
    black: 'black',
    white: 'white',
    green: 'green',
}

export {
    Colours,
    Container
}